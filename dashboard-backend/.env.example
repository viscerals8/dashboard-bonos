DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
JWT_SECRET=

# Opcional. En produccion, dominio(s) exacto(s) del frontend separados por
# coma (ej: https://bonos.miempresa.cl). Si se deja vacio/sin definir, se usa
# un regex permisivo para localhost pensado solo para desarrollo.
ALLOWED_ORIGINS=
